{
  "name": "mymuns/activity-log",
  "description": "A comprehensive Laravel 12+ package that provides request-response logging and model change tracking (versioning) with the ability to revert model states via command or API. Logs can be stored in the database or sent to external services like Logstash, Elasticsearch, Loki, or Sentry.",
  "type": "library",
  "license": "MIT",
  "authors": [
    {
      "name": "Erman Titiz",
      "email": "your-email@example.com"
    }
  ],
  "require": {
    "php": "^8.0",
    "illuminate/support": "^10.0 || ^11.0 || ^12.0",
    "spatie/laravel-http-logger": "^1.0 || ^2.0 || ^3.0"
  },
  "autoload": {
    "psr-4": {
      "ActivityLog\\": "src/"
    }
  },
  "minimum-stability": "stable",
  "prefer-stable": true,
  "extra": {
    "laravel": {
      "providers": [
        "ActivityLog\\ActivityLogServiceProvider"
      ]
    }
  }
}
